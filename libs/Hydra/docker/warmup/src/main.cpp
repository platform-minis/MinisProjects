/**
 * Szkic rozgrzewkowy dla obrazu Dockera.
 *
 * Nie robi nic i nie ma robić: chodzi wyłącznie o to, żeby PlatformIO
 * musiało uruchomić pełen łańcuch narzędzi — kompilator, linker, generator
 * obrazu — i pobrało go do wnętrza obrazu.
 */

#include <Arduino.h>

void setup() {}
void loop() {}
